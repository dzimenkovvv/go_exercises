Просто учебные задачи на Go
Делаю для практики и закрепления материала

Содержит:

Задача 1(chanel_and_wait_group) — генерация чисел в канал, суммирование с помощью WaitGroup

Задача 2(min_wait_group) — вывод имён в параллельных горутинах через WaitGroup

Задача 3(select_with_two_chan) — чтение из двух каналов (int и string) через select

Задача 4(sqrt_chan) — Worker Pool: возведение чисел в квадрат и вывод оригинала + результата

Задача 5(wait_grouop_context_graceful_shutdown) — Worker Pool с context.WithTimeout, завершение воркеров по таймауту

Задача 6 (sqrt_chan_graceful_shutdown) - Worker Pool с context.WithTimeout, генерация чисел с ограничением по времени и обработка в нескольких воркерах
